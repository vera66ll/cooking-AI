<view class="empty-state">
  <text class="empty-icon">{{icon}}</text>
  <text class="empty-text">{{text}}</text>
  <view class="empty-action" wx:if="{{showAction}}" bindtap="onActionTap">
    <text>{{actionText}}</text>
  </view>
</view>
