# ☑️ 云函数上传检查清单

## 📋 上传前准备

- [ ] 已开通云开发服务
- [ ] 环境ID已配置：`vera-liu-space-6g2h5zpq5043f8fe`
- [ ] 微信开发者工具已打开项目
- [ ] 网络连接正常

---

## 🚀 上传步骤（按顺序执行）

### ✅ 步骤1：设置云环境

在微信开发者工具中：

```
操作：右键点击 cloudfunctions 文件夹
     → 选择"当前环境"
     → 点击"vera-liu-space-6g2h5zpq5043f8fe"
     
完成标志：✓ 看到环境名称显示在菜单中
```

- [ ] 已完成环境设置

---

### ✅ 步骤2：上传 initDatabase（数据库初始化）

```
操作：右键点击 cloudfunctions/initDatabase 文件夹
     → 选择"上传并部署：云端安装依赖"
     → 等待上传完成
     
预计时间：15-20秒
```

**上传中...**
- [ ] 开始上传
- [ ] 等待依赖安装
- [ ] 看到"上传成功"提示

**验证：**
- [ ] 云开发控制台能看到 initDatabase 函数
- [ ] 状态显示"部署成功"

---

### ✅ 步骤3：上传 userLogin（用户登录）

```
操作：右键点击 cloudfunctions/userLogin 文件夹
     → 选择"上传并部署：云端安装依赖"
     → 等待上传完成
     
预计时间：15-20秒
```

**上传中...**
- [ ] 开始上传
- [ ] 等待依赖安装
- [ ] 看到"上传成功"提示

**验证：**
- [ ] 云开发控制台能看到 userLogin 函数
- [ ] 状态显示"部署成功"

---

### ✅ 步骤4：上传 getUserInfo（获取用户信息）

```
操作：右键点击 cloudfunctions/getUserInfo 文件夹
     → 选择"上传并部署：云端安装依赖"
     → 等待上传完成
     
预计时间：15-20秒
```

**上传中...**
- [ ] 开始上传
- [ ] 等待依赖安装
- [ ] 看到"上传成功"提示

**验证：**
- [ ] 云开发控制台能看到 getUserInfo 函数
- [ ] 状态显示"部署成功"

---

### ✅ 步骤5：上传 updateUserInfo（更新用户信息）

```
操作：右键点击 cloudfunctions/updateUserInfo 文件夹
     → 选择"上传并部署：云端安装依赖"
     → 等待上传完成
     
预计时间：15-20秒
```

**上传中...**
- [ ] 开始上传
- [ ] 等待依赖安装
- [ ] 看到"上传成功"提示

**验证：**
- [ ] 云开发控制台能看到 updateUserInfo 函数
- [ ] 状态显示"部署成功"

---

## 🧪 上传后测试

### 测试1：初始化数据库

在微信开发者工具的**控制台（Console）**中执行：

```javascript
wx.cloud.callFunction({
  name: 'initDatabase'
}).then(res => {
  console.log('结果:', res);
});
```

- [ ] 执行上述代码
- [ ] 看到返回结果 code: 0
- [ ] 看到5个集合创建成功

---

### 测试2：测试用户登录

刷新小程序页面，查看控制台：

- [ ] 看到"自动登录成功"日志
- [ ] 无错误信息
- [ ] 个人中心能显示用户信息

或手动测试：

```javascript
wx.cloud.callFunction({
  name: 'userLogin'
}).then(res => {
  console.log('登录结果:', res);
});
```

- [ ] 调用成功
- [ ] 返回用户信息

---

### 测试3：获取用户信息

```javascript
wx.cloud.callFunction({
  name: 'getUserInfo'
}).then(res => {
  console.log('用户信息:', res);
});
```

- [ ] 调用成功
- [ ] 返回完整的用户数据

---

## ✅ 最终验证清单

全部完成后，应该满足以下条件：

- [ ] ✅ 4个云函数都已上传
- [ ] ✅ 云开发控制台能看到所有函数
- [ ] ✅ 所有函数状态为"部署成功"
- [ ] ✅ 数据库已初始化（5个集合）
- [ ] ✅ 小程序运行无报错
- [ ] ✅ 控制台显示"自动登录成功"
- [ ] ✅ 个人中心显示用户信息

---

## 🎊 完成！

如果以上所有复选框都已勾选，恭喜您！🎉

CloudBase 云开发已成功集成，可以开始使用云端功能了。

---

## 📝 备注

**上传时间记录：**
- initDatabase: _________ (填写时间)
- userLogin: _________
- getUserInfo: _________
- updateUserInfo: _________

**遇到的问题：**
```
（记录任何错误或问题）
```

**解决方案：**
```
（记录如何解决）
```

---

## 💡 提示

- 每个云函数上传约需 15-20 秒
- 总耗时约 1-2 分钟
- 如遇失败，查看云函数日志排查
- 上传顺序不影响使用，但建议按此顺序

祝您顺利！🚀
